# gait — 歩行パターン生成（50Hz）

重心軌道と足先軌道を生成する。

Phase 4 では、この層を強化学習ポリシーで置き換えることになる。
どちらも `Controller` インターフェースを実装するので、**比較実験がそのまま行える**。

古典的な手法（線形倒立振子モデル＋ZMP）も一度実装しておくと、
学習ポリシーの挙動を評価する基準になる。学習だけやると
「なぜ動くのか / なぜ動かないのか」が判断できなくなる。
